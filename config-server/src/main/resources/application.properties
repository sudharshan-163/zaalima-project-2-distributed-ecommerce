spring.application.name=config-server
server.port=8888

# Eureka registration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Configuration repository location will be configured after the
# official project requirements are verified.
spring.cloud.config.server.git.uri=file://${CONFIG_REPO_PATH}
